{
  "name": "dipper",
  "version": "1.0.0",
  "description": "A state machine library for javascript",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "state",
    "machine",
    "statemachine",
    "states"
  ],
  "author": "Guilherme Brunow Gomes",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.12.18",
    "nodemon": "^1.18.9",
    "ts-node": "^7.0.1",
    "typescript": "^3.2.2"
  }
}