{
  "name": "dipper.js",
  "version": "0.0.1",
  "description": "A state machine library for TypeScript",
  "main": "index.js",
  "types": "lib/index",
  "scripts": {
    "start": "npm run build:live",
    "build": "rm -rf lib/* ; tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/examples/run-examples.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publisher": "npm run build ; npm publish",
    "publisher:force": "npm unpublish --force ; npm run publisher"
  },
  "keywords": [
    "state",
    "machine",
    "statemachine",
    "states"
  ],
  "repository": {
    "url": "https://github.com/gbrunow/dipper",
    "type": "git"
  },
  "author": "Guilherme Brunow Gomes",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.12.18",
    "node-emoji": "^1.8.1",
    "nodemon": "^1.18.9",
    "ts-node": "^7.0.1",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "rxjs": "^6.3.3"
  },
  "publishConfig": {
    "registry": "http://npm.caps.ua.edu/"
  },
  "files": [
    "/lib/dipper"
  ]
}